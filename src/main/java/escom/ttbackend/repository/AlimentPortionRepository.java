package escom.ttbackend.repository;

import escom.ttbackend.model.entities.AlimentPortion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlimentPortionRepository extends JpaRepository<AlimentPortion, Long> {
}
